import './NavBar.scss';

export default function NavBar(props) {
    return (
        <nav class="Navbar">
            <ul>
                <li>
                    <a href="/">Home</a>
                </li>
            </ul>
        </nav>
    );
}
